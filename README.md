# eurosat-land-classifier
Build a convolutional neural network (CNN) that classifies satellite images into different land-use categories(Forest, Residential, River, Highway, Agricultural, Industrial) using the EuroSAT dataset.
